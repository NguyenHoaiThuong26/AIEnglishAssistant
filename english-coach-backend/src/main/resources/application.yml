spring:
  application:
    name: english-coach-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/english_coach
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true

